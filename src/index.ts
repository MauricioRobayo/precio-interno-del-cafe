import { FileDownloader } from "./dowloader";
import { uploadFile } from "./uploader";

// const destFile = 'ref-price.pdf'
// const {eTag } = await repo.getLatest();
// const fd = new FileDownloader('eTag', destFile)
// await fd.downloadFileWithExponentialBackOff()
// await uploadFile(destFile)
